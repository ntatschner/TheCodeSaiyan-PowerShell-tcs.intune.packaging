name: 'CI Validate'
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  validate:
    uses: TheCodeSaiyan-PowerShell/tcs-shared-workflows/.github/workflows/powershell-validate.yml@main
    with:
      moduleName: 'tcs.intune.packaging'