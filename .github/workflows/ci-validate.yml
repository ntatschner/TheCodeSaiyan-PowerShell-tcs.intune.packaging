name: 'CI Validate'
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  validate:
    uses: ntatschner/tcs-shared-workflows/.github/workflows/powershell-validate.yml@main
    with:
      module_name: 'tcs.intune.packaging'
      module_path: 'modules/tcs.intune.packaging'